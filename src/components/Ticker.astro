---
const news = [
  "Microsoft announces GPT-5 integration for 2026",
  "Nvidia RTX 5090 sets new benchmark for local LLM inference",
  "OpenAI's o1 model reaches human-level logic in coding tests",
  "Google DeepMind releases high-efficiency AlphaCode 3",
];
---

<div class="ticker-wrap">
  <div class="ticker-label">
    <span class="pulse"></span>
    Breaking
  </div>
  <div class="ticker">
    <div class="ticker-content">
      {news.concat(news).map((item) => (
        <div class="ticker-item">{item}</div>
      ))}
    </div>
  </div>
</div>

<style>
  .ticker-wrap {
    height: 32px;
    background: var(--purple-dark);
    display: flex;
    align-items: center;
    overflow: hidden;
    position: relative;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .ticker-label {
    background: var(--purple-primary);
    color: var(--white);
    padding: 0 1rem;
    height: 100%;
    display: flex;
    align-items: center;
    font-size: 0.75rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    position: relative;
    z-index: 2;
    box-shadow: 10px 0 20px rgba(0, 0, 0, 0.3);
    gap: 0.5rem;
  }

  .pulse {
    width: 6px;
    height: 6px;
    background: var(--white);
    border-radius: 50%;
    animation: blink 1s infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(1.2); }
  }

  .ticker {
    flex: 1;
    overflow: hidden;
  }

  .ticker-content {
    display: flex;
    white-space: nowrap;
    animation: ticker 40s linear infinite;
  }

  .ticker-content:hover {
    animation-play-state: paused;
  }

  .ticker-item {
    display: inline-block;
    padding: 0 2rem;
    color: var(--white);
    font-size: 0.85rem;
    font-weight: 500;
    opacity: 0.9;
  }

  .ticker-item::after {
    content: "•";
    margin-left: 2rem;
    opacity: 0.3;
  }

  @keyframes ticker {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  @media (max-width: 768px) {
    .ticker-wrap { height: 28px; }
    .ticker-item { font-size: 0.75rem; padding: 0 1rem; }
  }
</style>
